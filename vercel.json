{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build && cp src/generated/prisma/*.node .next/server/",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "crons": [
    { "path": "/api/cron/billing", "schedule": "0 17 * * *" }
  ]
}
